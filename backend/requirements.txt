# RoyalBot 统一后端依赖
# 自建 Emby 服务器 + 用户门户 + 管理后台一体化

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 数据库
sqlalchemy>=2.0.23
alembic>=1.12.0
psycopg2-binary>=2.9.9
pymysql>=1.1.0

# 缓存
redis>=5.0.0

# 认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# 配置与工具
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.1.0
python-dotenv>=1.0.0
httpx>=0.25.0
aiofiles>=23.2.0
apscheduler>=3.10.0

# 转码（外部二进制，需系统安装 ffmpeg）
# ffmpeg 由宿主系统/Docker 镜像提供

# 监控
prometheus-client>=0.19.0

# 日志
loguru>=0.7.0

# 性能
orjson>=3.9.0
